# =============================================================================
# DevHub API - Environment Configuration Template
# =============================================================================
# Copy this file to create your environment-specific configurations:
#   - .env.development  (local development)
#   - .env.certification (pre-production testing)
#   - .env.production   (production deployment)
#
# Replace all placeholder values with your actual credentials
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# MongoDB Configuration
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb+srv://YOUR_DB_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/devhub-ENV?retryWrites=true&w=majority
MONGODB_TEST_URI=mongodb+srv://YOUR_DB_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/devhub-test?retryWrites=true&w=majority

# JWT Configuration
# Generate strong secrets for each environment (use: openssl rand -base64 32)
JWT_SECRET=CHANGE_THIS_SECRET_MINIMUM_32_CHARS
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=CHANGE_THIS_REFRESH_SECRET_MINIMUM_32_CHARS
JWT_REFRESH_EXPIRES_IN=7d

# Rate Limiting
# Development: 900000ms (15 min) / 100 requests
# Certification: 600000ms (10 min) / 200 requests
# Production: 300000ms (5 min) / 50 requests
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
# Development: http://localhost:4200
# Certification: https://cert.devhub.example.com
# Production: https://devhub.example.com
CORS_ORIGIN=http://localhost:4200

# Logging
# Development: debug | Certification: info | Production: error
LOG_LEVEL=debug
ENABLE_REQUEST_LOGGING=true

# Feature Flags
# Development/Certification: true | Production: false (Swagger)
ENABLE_SWAGGER=true
ENABLE_RATE_LIMITING=true

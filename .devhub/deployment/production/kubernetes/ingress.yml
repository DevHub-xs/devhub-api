apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devhub-api-prod-ingress
  namespace: devhub-prod
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rate-limit: "100"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - api.devhub.example.com
    secretName: devhub-api-prod-tls
  rules:
  - host: api.devhub.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devhub-api-prod-service
            port:
              number: 80
